#include "no_style.hpp"

namespace GooBalls {
using namespace d2;

void NoStyle::shapeScene(const Physics::Scene& physScene, Render::Scene& renderScene) const {
    // just do nothing, keep as it is
}

}
